import React from "react";

function RestrictedHolidaystab() {
  return (
    <div>
      Restricted Holidays(HR)are a set of holidays allocated by the company that
      are optional for the employee to utilize.The company sets a limit on the
      amount of holidays that can be used.
    </div>
  );
}

export default RestrictedHolidaystab;
